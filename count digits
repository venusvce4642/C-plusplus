#include<bits/stdc++.h>
using namespace std;
int count(int n)
{
	int c=0;
	while(n>0)
	{
		c++;
		n/=10; //by dividing 10 we can remove the last digit
	}
	return c;
}

int main()
{
	int n;
	cout<<"enter the number:";
	cin>>n;
	cout<<"Number of digits in given number is : "<<count(n);
}
